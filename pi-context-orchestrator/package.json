{
  "name": "pi-context-orchestrator",
  "version": "0.1.0",
  "description": "Context pressure monitor, task router, and model selector for Pi — keeps main agent focused by delegating unrelated work to subagents with optimal model selection",
  "main": "index.ts",
  "keywords": ["pi-package"],
  "pi": {
    "name": "pi-context-orchestrator",
    "description": "Context orchestrator — monitors context pressure, routes irrelevant tasks to subagents with model selection, triggers save-and-compact at threshold"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "latest",
    "typebox": "^1.3.6"
  }
}
